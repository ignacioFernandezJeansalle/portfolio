---
const { class: classInput } = Astro.props;
const className = `flex gap-2 items-center px-3 py-1 rounded-full text-sm ${classInput ? classInput : ""}`;
---

<li class={className}>
  <slot />
</li>
